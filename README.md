# dotfiles
我在 Ubuntu 下使用的 dotfiles，包括 shell 的配置和 vim 的配置。

## 使用方法

```
$ sudo yum install git zip vim
$
$ cd ~
$ git clone https://github.com/morgengc/dotfiles.git
$ cp dotfiles/dotfiles_ubuntu.zip ~
$ unzip dotfiles_ubuntu.zip
```

退出当前 shell 后重新打开 shell 即可生效。

